
Charlie <- 1:5
Charlie

#give names 
names(Charlie) <- c("a", "b", "c", "d", "e")
names(Charlie)
Charlie

#clear name
names(Charlie) <- NULL

#NAming matrix dimensions
temp.vec <- rep(c, 3)
c <- c("a", "b", "Zz")

temp.vec


Bravo <- matrix(temp.vec, 3, 3)
Bravo

#naming rows in the matrix

rownames(Bravo) <- c("how", "are", "you")
Bravo

colnames(Bravo) <- c("X", "Y", "Z")
